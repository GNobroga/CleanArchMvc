@model ProductDTO

<div style="display: flex; gap: 1rem; margin-left: 1rem;">
    id (@Model.Id) | @Model.Name
    <a asp-action="Update" asp-route-id="@Model.Id">Atualizar</a>
    <a asp-action="Remove" asp-route-id="@Model.Id">Remover</a>
</div>

